//Write a C program to display the pyramid pattern using the alphabet

#include<stdio.h>

int main()
{
int i,j,k,n;
printf("enter number of rows:");
scanf("%d",&n);

for(i=1;i<=n;i++)
{
    for(j=1;j<=i;j++)

return 0;
}
